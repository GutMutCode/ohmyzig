This folder contains Win32-specific C sources.

- `win32_ui.c` — window class registration, window creation, and message loop.

Headers live in `inc/` so they can be consumed via `@cImport` from Zig.

